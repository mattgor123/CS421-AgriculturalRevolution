package rev.model.core;

/**
 * A dynamic entity.
 * 
 *
 * 
 */
public interface ModelDynamic {

    /**
     * Updates the entity.
     * 
     * @param model
     *            the model.
     */
    public void update(ModelDelegate model);
}